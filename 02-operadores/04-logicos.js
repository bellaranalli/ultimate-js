// AND, OR, NOT

// AND &&
let mayor = false;
suscrito = true;
//console.log(true && true); //true
//console.log(false && true); //false
console.loh('operador and', mayor && suscrito);

// OR ||
console.loh('operador or', mayor || suscrito); // devuelve true si es que al menos una de las dos variables evalua en true, devuelve false si las dos son false

// NOT !
console.loh('operador not', !mayor); 
let catalogoInfantil = !mayor;